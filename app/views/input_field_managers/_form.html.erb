<%= form_for(@input_field_manager) do |f| %>
  <% if @input_field_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@input_field_manager.errors.count, "error") %> prohibited this input_field_manager from being saved:</h2>

      <ul>
      <% @input_field_manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <%= f.fields_for :input_field_manager_values do |builder| %>
    <%= render 'input_field_manager_values_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields "Add Input Field Values", f, :input_field_manager_values %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
